version: 2

jobs:
  build:
    docker:
      - image: alexwijn/docker-git-php-composer
    working_directory: ~/repo
    branches:
      only:
        - master
        - dev
    steps:
      - deploy:
          command: |
            if [ "${CIRCLE_BRANCH}" == "dev" ]; then
              ssh -o "StrictHostKeyChecking no" ${USER}@dev.cg.providencedenver.org 'cd /home/pbc_cg/dev.cg.providencedenver.org && touch circleworksnowagain.txt'
            fi
